<a href="#/details/:{{id}}" class="added-destination">
    <img src={{imageUrl}}
        alt="" class="picture-added-destination">
    <h3>{{destination}}</h3>
    <span>to {{city}} </span><span>{{depDate}}</span>
</a>